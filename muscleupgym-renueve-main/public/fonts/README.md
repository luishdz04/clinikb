# Fonts

Esta carpeta es para fuentes personalizadas si decides usar tipografías locales en lugar de Google Fonts.

## Recomendaciones:
- Usar formatos web: WOFF2, WOFF
- Declarar las fuentes en `globals.css` usando `@font-face`
- Next.js también soporta `next/font` para optimización automática
